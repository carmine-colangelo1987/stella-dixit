/** @format */

export enum AppRoutes {
  BASE = '/',
  NEW_MATCH = '/nuova-partita',
  CREATE_PLAYERS = '/cercatori/creazione',
  SELECT_PLAYER = '/cercatori/seleziona',
  CURRENT_MATCH = '/partita',
}
