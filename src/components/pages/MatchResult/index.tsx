/** @format */

import Container from '../../common/Container';
import Coin from '../../common/Coin';

const MatchResult = () => {
  return (
    <Container>
      <Coin color={'teal'} />
      <Coin color={'teal'} perspective />
    </Container>
  );
};

export default MatchResult;
